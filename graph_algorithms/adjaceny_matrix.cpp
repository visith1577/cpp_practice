#include <iostream>

using namespace std;

// In the matrix for non-weighted if no node? 0 : 1
// for weighted if no node? Inf: weight
// matrix create of dim |v|*|v|
// vertex list is created
// worst case time complexity for finding adjacent node O(|v|) + O(|v|)
// if vertex list is a map this time complexity is reduced to O(|v|)
// find if two nodes are connected O(1)
// this s good if graph is dense or v^2 is too little to matter


int main()
{

}